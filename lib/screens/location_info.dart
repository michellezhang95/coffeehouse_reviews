import 'package:flutter/material.dart';

//listview listtiles for each cafe
class LocInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Location Details')),
    );
  }
}
